from vortex.Tuple import TupleField, Tuple


class ChunkedIndexServerStatusTupleABC(Tuple):

    queueStatus: bool = TupleField(False)
    queueSize: int = TupleField(0)
    queueProcessedTotal: int = TupleField(0)
    queueLastError: str = TupleField()
