[buildout]

extends =
    buildout.cfg

parts += cmds
         pydev

[cmds]
recipe = collective.recipe.cmd
on_install=true
on_update=true
cmds = cp $HOME/.pydevproject $HOME/.project parts/omelette

[pydev]
recipe = pb.recipes.pydev
pydevproject_path = /home/wesleybl/workspaces/collective.cover/run/.pydevproject
eggs =
    ${instance:eggs}
    ${test:eggs}
    plone.recipe.zope2instance
