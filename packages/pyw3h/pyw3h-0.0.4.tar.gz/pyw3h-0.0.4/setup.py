import setuptools

setuptools.setup(
    name="pyw3h",  # Replace with your own username
    version="0.0.4",
    packages=setuptools.find_packages(),
    include_package_data=True,
    python_requires='>=3.9',
)
