"""the command we write in AM just cleaner

btw if I say a parameter must be a certain type it means it must be convertable to that type using magic functions
    >>> x must be an int
    >>> if a float is passed to x
    >>> x will be int(x)"""

from AMcomandPack import geo