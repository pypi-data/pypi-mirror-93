# TODO get rid of regex req ideally so we can have a more relaxed requirement
regex==2019.11.1
# TODO Figure out if there's a more stable implementation so we can remove upper bound
cytoolz>=0.10.1,<0.11
