image: python:3.7

before_script:
  - pip install tox

stages:
  - style
  - build
  - coverage
  - deploy

# style
flake8:
  stage: style
  script:
    - tox -vv
  variables:
    TOXENV: flake8
  tags:
    - docker

# build
build:
  stage: build
  script:
    - python setup.py sdist bdist_wheel
  artifacts:
    paths:
      - dist/
    expire_in: 1 week
  tags:
    - docker

# coverage
coverage:
  stage: coverage
  script:
    - tox -vv
  variables:
    TOXENV: coverage
  tags:
    - docker

# deploy
deploy:
  stage: deploy
  dependencies:
    - build
  script:
    - pip install twine
    - twine upload -u $PYPI_USER -p $PYPI_PASSWORD dist/*
  only:
    - tags
  tags:
    - docker
