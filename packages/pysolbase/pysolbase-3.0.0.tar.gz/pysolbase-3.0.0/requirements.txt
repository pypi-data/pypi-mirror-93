# PYPI :
# workon pysolbase
# python setup.py sdist && twine check dist/* && twine upload dist/*; rm -rf './dist'; rm -rf pysol*.egg-info; echo "OVER"

# gevent stack
greenlet
gevent

# distro
distro

# date
pytz
