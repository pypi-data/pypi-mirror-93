{{stage}} = KubernetesPodOperator(
               image='{{img}}',
               cmds=['{{cmd}}'],
               namespace='{{namespace}}',
               name='airflow-{{stage}}',
               is_delete_operator_pod=True,
               in_cluster=True,
               task_id='{{stage}}',
               get_logs=True,
               dag=dag
           )